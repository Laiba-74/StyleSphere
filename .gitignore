# Node modules (both client + server)
node_modules
client/node_modules
server/node_modules

# Environment files (keep keys secret)
.env
client/.env
server/.env

# React build output (only created when you build frontend)
client/build

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS/system files
.DS_Store
